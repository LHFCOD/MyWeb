<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hongtu.slice.mapper.UserMapper">

    <!-- 查询网校的用户数目 -->
    <select id="getAllSubject" parameterType="Long"  resultType="com.hongtu.slice.db.model.generated.Subject">
        SELECT * from tb_subject limit 10
    </select>
</mapper>