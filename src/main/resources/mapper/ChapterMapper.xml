<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hongtu.slice.mapper.UserMapper">

    <!-- 查询网校的用户数目 -->
    <select id="getChapterByPid" parameterType="Int"  resultType="com.hongtu.slice.db.model.generated.Chapter">
        SELECT * from tb_catalog where pid= #{pid}
    </select>
</mapper>